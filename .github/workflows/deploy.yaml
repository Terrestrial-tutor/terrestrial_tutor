name: Deploy

on:
  push:
    branches: [ "changes" ]
  pull_request:
    branches: [ "changes" ]

jobs:

  deploy:

    runs-on: self-hosted

    steps:
        - uses: actions/checkout@v4 
          with:
            path: /home/tutor/terrestrial_tutor
        - name: Deploy changes
          working-directory: ./
          run: docker compose restart nginx
